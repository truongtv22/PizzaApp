import Toast from "../Toast/Toast"

const ZoomUs = {
  initialize() {
    Toast.showText("Zoom không hỗ trợ trên nền tảng này")
    return Promise.resolve()
  },

  startMeeting() {
    Toast.showText("Zoom không hỗ trợ trên nền tảng này")
    return Promise.resolve()
  },

  joinMeeting() {
    Toast.showText("Zoom không hỗ trợ trên nền tảng này")
    return Promise.resolve()
  },
}

export default ZoomUs
